document.addEventListener("DOMContentLoaded",()=>{const id=Number(new URLSearchParams(location.search).get("id"))||1;const p=PRODUCTS.find(x=>x.id===id)||PRODUCTS[0];document.title=p.name+" — LOREM";document.getElementById("productPage").innerHTML=`<section class="product-detail"><div class="detail-image ${p.bg}"><span>${p.emoji}</span></div><div class="detail-copy"><p class="eyebrow">${p.category}</p><h1>${p.name}</h1><div class="detail-price">${money(p.price)}</div><p class="detail-desc">${p.desc}</p><div class="quantity"><button id="minus">−</button><span id="qty">1</span><button id="plus">+</button></div><button class="btn dark add-btn" id="add">ADD TO BAG →</button><div class="product-note">♡ Packed with care<br>♡ Easy gifting<br>♡ Free shipping over ₹999</div></div></section>`;let q=1;const qty=document.getElementById("qty");document.getElementById("minus").onclick=()=>{q=Math.max(1,q-1);qty.textContent=q};document.getElementById("plus").onclick=()=>{q++;qty.textContent=q};document.getElementById("add").onclick=()=>addToCart(p.id,q)});